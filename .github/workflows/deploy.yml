name: Obsolete Cloudflare Worker deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled for current Railway app layout
    runs-on: ubuntu-latest
    steps:
      - name: Explain current deploy path
        run: |
          echo "This repo now uses a backend/ FastAPI Railway deployment."
          echo "The old Cloudflare Worker workflow expected apps/web and a root pnpm package.json."
          echo "Use Railway for the current live deployment."
