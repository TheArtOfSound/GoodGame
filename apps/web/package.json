{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply goodgame --local",
    "db:migrate:remote": "wrangler d1 migrations apply goodgame --remote",
    "db:seed:local": "wrangler d1 execute goodgame --local --file=./seed/seed.sql && wrangler d1 execute goodgame --local --file=./seed/enrich.sql",
    "db:seed:remote": "wrangler d1 execute goodgame --remote --file=./seed/seed.sql && wrangler d1 execute goodgame --remote --file=./seed/enrich.sql",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "fflate": "^0.8.3",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.95.0"
  }
}
